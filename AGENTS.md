# Nanikiru 项目协作说明

## 开始工作前

- 阅读 `docs/design.md`、`docs/training-product.md`、`docs/rules-and-settlement.md`、`docs/game-environment.md`、`docs/efficiency-review.md`、`docs/yaku-decisions.md`、`docs/defense.md` 、`docs/tenpai-analysis.md` 和 `docs/phase-1.md`，确认当前目标、验收要求和已有实现。
- 其他对话不一定拥有原始聊天上下文，以项目文档和实际代码为依据。
- 区分用户已确认的决定与尚未确认的设计建议，不要把建议视为定案。
- 完成相关工作后同步更新阶段进度；仅把实际实现并验证的内容标为完成。

## 项目目标

- 主要目的是锻炼模型训练、数据处理、接口调用与封装能力。
- 产品目标改为桌面何切训练软件：随机开局，玩家与三家训练好的机器人对局，切出后展示模型的该切评分，记录逐切评分并汇总整局训练评分。详见 `docs/training-product.md`；基础牌效分析与逐切复盘已实现，模型及综合评分尚未实现；已接入确定性基础牌效机器人。
- 摄像头监控与何切推荐调整为共享评估能力的后续应用，先雀魂画面再手机摄像头，智能眼镜是远期方向；视觉不再是训练软件交付的前置条件。
- 决策评分与麻将番符点数结算分开。模型只接收行动前该玩家可见信息，不得接收真实牌山、种子或对手暗手；逐切评分量纲和整局聚合方法尚待确认，不提前写死。
- 当前采用 Python 为主的方向。界面服务于游戏操作、数据校对与训练调试；当前新目标要求清晰简洁的图形牌桌，不能继续仅以大段文字展示局面。
- 当前阶段（2026-09-16）：已实现随机/种子开局、真实牌山、四家人工响应、吃碰杠、立直、和牌、流局及单局结算；图形牌桌入口 `run_game.py`，支持理牌、四玩家视角和全知调试。规则基准及验证见 `docs/rules-and-settlement.md`。
- 当前默认庄家人工、三家基础立直策略机器人（可切换旧基准），允许关闭机器人恢复全人工调试；无连续多局调度或模型评分；机器人可切换基础牌效与役种感知，后者按保守基准允许吃碰；旧基准均合法和牌自动接受，不主动杠或立直，基准见 `docs/yaku-decisions.md`；已增加基础牌效比较、逐切记录及汇总，见 `docs/efficiency-review.md`，不能称为综合最优。具体边界采用文档中说明的开发默认，不宣称完全复刻雀魂。后续任务不得以旧阶段“暂不处理特殊规则”限制已扩展的 Game。
- 算分依赖固定 `mahjong==2.0.0`，通过 `scoring.py` 封装；本地负责响应、振听、牌山和结算支付。使用项目 `.venv/Scripts/python.exe`，或先在目标解释器执行 `python -m pip install mahjong==2.0.0`；从项目目录运行无需安装项目本体。旧 Recorder 本身的语义和测试保持兼容。
- 新阶段已确认：默认四人东但场风参数不能写死；先单局结算并保留多局结果信息；三家依次响应后统一裁决；评估算分库，可参考算法本地实现；考虑赤宝与新杠宝开关。非法动作保留最后合法局面及已有合法响应，显示原因并暂停；修正或撤销后显式恢复，不强行放行、不自动扣分。关闭赤宝保留赤五身份和牌面，只取消赤宝加番。
- 环境内部保存四家真实手牌和完整牌山；通过独立玩家观察接口输出可见信息。仅把牌面隐藏在 UI 上不满足信息隔离要求；未来机器人不能获得完整状态、种子或牌山。
- 保留现有记录库、文件和测试；观察记录与全知游戏状态各自有明确用途，不通过删除原有未知手牌校验来冒充完整环境。
- 第一阶段核心为独立于图形界面的牌局记录库，具体范围见阶段文档。用户另已授权简单 Tk 调试窗口，用于动作录入、数据显示与测试输出；核心不得依赖窗口。不要提前加入视觉或模型训练。
- 已有首次交付是普通摸打记录闭环，通过 Python 脚本调用；支持从自家摸牌后待弃牌的完整快照中途接入，不要求开局历史。此限制属于旧记录入口，不限制新环境从洗牌发牌开始。
- 初始快照可含已有副露与立直；首次交付不支持新增鸣牌或立直事件，遇到这些动作须明确拒绝，不跳过。
- 旧记录器根据完整快照推导剩余可摸牌数，耗尽不自动结算；新 Game 按实际牌山计算，在最后弃牌响应结束后进行流局/和牌结算。测试暂停不是麻将流局。新游戏存档为 v7（含独立役种、防守与听牌分析记录），兼容加载 v2/v3/v4/v5/v6；旧版缺失听牌分析或自身振听信息时保持缺失并提示受限，旧操作来源标为 unknown-legacy，旧 Game v1 明确拒绝，旧 Recorder v1 使用旧入口。

- 已实现手动进攻／弃和及逐家现物依据，详见 `docs/defense.md`。仅以成立立直为明确威胁；保留原进攻排序及基础牌效统计，不把未知当安全。人工评估模式与机器人模式独立；基础机器人不应用模式。

## 开发原则

### 编码前先思考

- 明确假设、输入输出和验收标准。
- 遇到影响正确性或范围的歧义，说明不同解释并澄清，不默默选择。
- 如果存在更简单的方案，说明取舍。
- 多步骤工作先给出简短计划和对应验证方式。

### 简洁优先

- 只实现当前任务需要的功能，不为猜测中的未来需求添加抽象、配置或服务。
- 优先使用 Python 标准库；新增依赖应有具体理由。
- 模块先通过普通 Python 调用协作，需要外部访问时再增加网络接口。

### 小范围修改

- 阅读现有代码后逐步修改，不删除原有文件从零重写。
- 保留已实现行为，遵循现有风格，不顺便重构或清理无关代码。
- 清理本次修改产生的无用内容；发现原有无关问题时单独说明。

### 以验证结果为准

- 修改对应明确的验收标准；修复问题时优先构造可复现案例。
- 非法动作不能部分修改状态或污染历史。
- 状态更新、保存加载和回放应得到一致结果。
- 不把未知信息当成已知事实，不把推测结果写进事实记录。
- 不把“成功记录动作”表述成“已通过全部麻将规则校验”。

- 已实现独立听牌质量分析与“听牌质量”复盘页，见 `docs/tenpai-analysis.md`。不改变机器人选择和基础牌效统计；条件算分不含本场、供托、责任支付，不是期望收益。

- 日常训练 UI 已整理：设置和调试默认隐藏，人工弃牌反馈固定保留，复盘选择不随机器人推进跳转；入口与验证见 `docs/ui-training.md`。不要用核心策略变化实现界面美化。

- 新增 `tenpai_comparison.py` 和 `comparison_ui.py`，只从已保存分析派生比较，不重新推荐、不改变机器人或存档；讲解停顿是独立 UI 状态，不能用核心 resume 跳过暂停。接口和状态处理见 `docs/tenpai-comparison.md`。

- 开发者预览版新增一向听条件推进、保守立直策略和 UI 仅有过自动响应。详见 `docs/developer-preview.md`；新策略先沿用役种/防守选择弃牌，再比较同一张牌的立直，不改变旧基准排序。默认基础立直；旧基准仍不主动立直。`riichi_context` 只给公开宣言条件；v7 新评价保存在原决策记录，旧记录不补造。推进仅用于解释、按需后台计算。
